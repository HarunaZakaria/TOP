// write a function that add two different numbers


//defining the function the addition function
function add(a, b){
    const sum = a + b;
    return sum;
}

// calling the addition function
let newSum = add(23, 34);
//alert(newSum);

// calling the multiplication function
function product(c, d){
    const multiply = c * d;
    return multiply;
}

let newMultip = product(5, 10);
alert(newMultip);